# Production build configuration for the admin SPA.
# Cloudflare Pages reads VITE_* env vars from its dashboard at build time;
# this file is the committed reference of what those values should be.
VITE_API_BASE_URL=https://kamos.fly.dev
