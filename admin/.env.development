# Local dev (vite dev server). Empty = same-origin: the SPA calls relative
# /v1/*, which vite.config.ts proxies to the local API at localhost:8080.
# Mirrors the production Pages-Function proxy so cookies stay first-party.
VITE_API_BASE_URL=
